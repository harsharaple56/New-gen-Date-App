/**
 * Primary app button. Thin alias over the design-system `PrimaryButton` so the
 * rest of the app can import a conventionally-named `Button`.
 */
export { default } from './PrimaryButton';
export type { ButtonVariant } from './PrimaryButton';
